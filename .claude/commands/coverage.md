Hiển thị coverage matrix.

Đọc generated tests (scan test files) + OpenAPI spec.
Map: mỗi endpoint × mỗi test layer → có test hay không.
Output: table format + coverage % + gap list.
Không cần chạy test — chỉ analyze test files.
